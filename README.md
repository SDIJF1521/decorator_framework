# 🔧 ClassNucleus 类注册与调度库

ClassNucleus 是一个基于 Python 元类机制的轻量级类注册库。它允许您为类设置唯一的 `fun_name` 属性，并将其自动注册到全局注册表中。注册完成后，您可以通过 `fun_name` 动态查找并实例化类，非常适合构建插件系统、任务调度系统等。

---

## 🧩 特性

- ✅ 自动注册类（无需手动添加到注册表）
- ✅ 支持通过字符串名称动态实例化类
- ✅ 注册冲突检查与注册表清理功能
- ✅ 简洁可扩展，便于构建插件架构
